# ADR-006: Autonomy is policy based

**Status:** Accepted

Tools are classified R0 through R4. Tenant autonomy and deterministic policy decide whether an action is allowed, requires approval, or is denied. No deployment mode disables all governance.
