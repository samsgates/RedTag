# Integration tests

The full integration suite is intended to run against the Docker Compose PostgreSQL service in CI. Add connector-specific contract suites under this directory when enabling real ERP/WMS/CRM integrations.
